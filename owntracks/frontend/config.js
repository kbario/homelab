window.owntracks = window.owntracks || {};
window.owntracks.config = {
  // Default baseUrl (same host) works: frontend nginx proxies API to recorder
  // via SERVER_HOST/SERVER_PORT. Access at https://owntracks.<tailnet>.ts.net
};
